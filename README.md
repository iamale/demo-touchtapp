A [react-tap-event-plugin](https://github.com/zilverline/react-tap-event-plugin) bug reproduction.

Build
------

```bash
[sudo] npm install -g cordova gulp
cordova platform add android
npm install
gulp build-run
```
